# components/content-ops

UI components for Content Ops (batch content).

**Rule:** NEVER import from `app/research/steps/` or from `components/tabs` or research-specific lib.  
`app/research/steps/` must NEVER import from `components/content-ops/` or `app/content-ops/`.
